import { View, Text } from "react-native";

export default function Info(){
    return(
        <View style={{flex: 1, padding: 20, alignItems: "center", justifyContent: "center"}}>
            <Text style={{fontSize: 30}}>Tela info</Text>
        </View>
    )
}